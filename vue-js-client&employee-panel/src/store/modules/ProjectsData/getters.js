const getters = {
    ProjectData(state) {
        return state.ProjectData;
    },   

    ProjectDetails(state) {
        return state.ProjectDetails;
    }, 
    ProjectemployeDetails(state) {
        return state.ProjectemployeDetails;
    },

    // commentsData(state) {
    //     return state.commentsData;
    // },

    EmpProjectData(state) {
        return state.EmpProjectData;
    },

    EmpProjectDetails(state) {
        return state.EmpProjectDetails;
    }
}

export default getters;